const encryptionService = require('../../Services/EncryptionService');

module.exports = {
    userAuth: async function (user){
        try{
            
            let User = await function(user){
             return user.userRole == "user"   
            }
        }
        catch{
        }
    }
}
